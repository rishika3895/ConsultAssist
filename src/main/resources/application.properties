spring.datasource.url=jdbc:mysql://localhost:3306/consultassist?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
# Update these credentials with your MySQL username and password
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Changed port to 8082 since 8081 is already in use
server.port=8082

# JWT Secret - in production, use a more secure method
jwt.secret=mySecretKey